Compile with `mvn package`, then run with `java -jar target/lox-1.0-SNAPSHOT.jar <filename>`

(To run the `GenerateAst` code-generation, instead run `java -cp target/lox-1.0-SNAPSHOT.jar com.craftinginterpreters.tool.GenerateAst <output_dir>`)
